services:
  - type: web
    name: garage-bot
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python garage_bot.py"
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: GROUP_CHAT_ID
        sync: false